# -*- encoding : utf-8 -*-
#class Subdomain
#  def self.matches?(request)
#    request.subdomain(TLD_SIZE).present? && request.subdomain(TLD_SIZE) != "www" && request.subdomain(TLD_SIZE) != "dev"
#  end
#end
